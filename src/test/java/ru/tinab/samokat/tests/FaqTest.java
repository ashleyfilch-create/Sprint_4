package ru.tinab.samokat.tests;

import org.junit.Assert;
import org.junit.Test;
import ru.tinab.samokat.pages.MainPage;

public class FaqTest extends BaseTest {

    @Test
    public void faqAllQuestionsOpenCorrectly() {
        MainPage mainPage = new MainPage(driver);

        // Открываем главную страницу
        mainPage.openPage();

        // Закрываем баннер cookie, если он есть
        mainPage.closeCookieBanner();

        // Скроллим к первому вопросу FAQ
        mainPage.scrollToFaq();

        // Массив с ожидаемыми текстами ответов FAQ
        String[] expectedAnswers = {
                "Сутки — 400 рублей. Оплата курьеру — наличными или картой.",
                "Пока что у нас так: один заказ — один самокат. Если хотите покататься с друзьями, можете просто сделать несколько заказов — один за другим.",
                "Допустим, вы оформляете заказ на 8 мая. Мы привозим самокат 8 мая в течение дня. Отсчёт времени аренды начинается с момента, когда вы оплатите заказ курьеру. Если мы привезли самокат 8 мая в 20:30, суточная аренда закончится 9 мая в 20:30..",
                "Только начиная с завтрашнего дня. Но скоро станем расторопнее.",
                "Пока что нет! Но если что-то срочное — всегда можно позвонить в поддержку по красивому номеру 1010.",
                "Самокат приезжает к вам с полной зарядкой. Этого хватает на восемь суток — даже если будете кататься без передышек и во сне. Зарядка не понадобится.",
                "Да, пока самокат не привезли. Штрафа не будет, объяснительной записки тоже не попросим. Все же свои.",
                "Да, обязательно. Всем самокатов! И Москве, и Московской области."
        };

        // Проходим по всем 8 вопросам FAQ
        for (int i = 0; i < expectedAnswers.length; i++) {
            mainPage.clickFaqQuestion(i);

            // Проверяем, что ответ отображается
            Assert.assertTrue(
                    "Ответ на вопрос FAQ с индексом " + i + " не отображается",
                    mainPage.isFaqAnswerDisplayed(i)
            );

            // Проверяем, что текст ответа совпадает с ожидаемым
            String actualText = mainPage.getFaqAnswerText(i);
            Assert.assertEquals(
                    "Текст ответа FAQ с индексом " + i + " не совпадает",
                    expectedAnswers[i],
                    actualText
            );
        }
    }
}